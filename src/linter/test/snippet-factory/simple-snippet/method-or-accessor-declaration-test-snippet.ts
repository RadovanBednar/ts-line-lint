import { createMultilineString } from '../../../../utils/text-utils';

export const methodOrAccessorDeclarationSnippet = createMultilineString(
    'class Foo {',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  constructor() {',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  public constructor() {',
    '    // implementation',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  protected constructor() {',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  private constructor(private firstDependency: FirstDependency,',
    '                      private secondDependency: SecondDependency,',
    '                      private thirdDependency: ThirdDependency) {',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  public static foo(): bar {',
    '    // implementation',
    '',
    '    // return statement',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  protected bar(protected firstParam: VeryLongTypeName,',
    '                protected secondParam: EvenLongerTypeName,',
    '                protected thirdParam: SomeMonstrouslyLongTypeName): bar {',
    '    // implementation',
    '',
    '    // return statement',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  public static excruciatinglyLongMethodName<PerhapsWithSomeLongGenerig>(',
    '                protected firstParam: VeryLongTypeName,',
    '                protected secondParam: EvenLongerTypeName): bar {',
    '    // implementation',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  @HostListener("click", ["$event.target"])',
    '  public onClick(btn) {',
    '    // implementation',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  private baz(): void {',
    '    // implementation',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  get foo() {',
    '    return this _foo;',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  @Input()',
    '  set foo(param: type) {',
    '    this _foo = param;',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '%BLANK_BEFORE%',
    '  @Input() set foo(param: type) {',
    '    this _foo = param;',
    '  }',
    '%BLANK_AFTER%',
    '  // non-blank line',
    '}',
    ''
);
